{
  "name": "tree-sitter-nix",
  "version": "0.5.0",
  "description": "Tree Sitter grammar for Nix",
  "main": "bindings/node",
  "files": [
    "binding.gyp",
    "bindings/node/**",
    "queries/*",
    "src/**",
    "grammar.js",
    "tree-sitter.json",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "build": "npm run generate && node-gyp build",
    "build-wasm": "tree-sitter build -w",
    "generate": "tree-sitter generate --abi 15",
    "prepublishOnly": "npm run build && npm run build-wasm",
    "test": "tree-sitter test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/numtide/tree-sitter-nix.git"
  },
  "author": "Charles Strahan",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/numtide/tree-sitter-nix/issues"
  },
  "homepage": "https://github.com/numtide/tree-sitter-nix#readme",
  "dependencies": {
    "nan": "^2.17.0"
  },
  "devDependencies": {
    "tree-sitter-cli": "0.25.10"
  },
  "tree-sitter": [
    {
      "file-types": [
        "nix"
      ],
      "highlights": [
        "queries/highlights.scm"
      ],
      "injection-regex": "^(nix)$",
      "locals": [
        "queries/locals.scm"
      ],
      "scope": "source.nix"
    }
  ]
}
